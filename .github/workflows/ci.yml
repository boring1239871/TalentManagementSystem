name: CI/CD Pipeline
on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      
    - name: Set up Java
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'temurin'
        
    - name: Build application
      run: mvn clean package -DskipTests
      
    - name: Generate deployment guide
      run: |
        echo "## 🚀 部署指南" > DEPLOY.md
        echo "" >> DEPLOY.md
        echo "### 方法1: Railway 部署" >> DEPLOY.md
        echo "1. 访问 https://railway.app" >> DEPLOY.md
        echo "2. 连接 GitHub 仓库" >> DEPLOY.md
        echo "3. 自动部署完成" >> DEPLOY.md
        echo "" >> DEPLOY.md
        echo "### 方法2: 本地运行" >> DEPLOY.md
        echo "下载 JAR 包: https://github.com/${{ github.repository }}/actions" >> DEPLOY.md
        echo "运行: \`java -jar hr-management-system-0.0.1-SNAPSHOT.jar\`" >> DEPLOY.md
        
    - name: Upload artifacts
      uses: actions/upload-artifact@v2
      with:
        name: hr-system-jar
        path: target/*.jar
